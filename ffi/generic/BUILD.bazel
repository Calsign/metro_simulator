load("@rules_rust//rust:defs.bzl", "rust_shared_library", "rust_static_library")

rust_shared_library(
    name = "engine_shared",
    srcs = ["ffi_wrapper.rs"],
    deps = ["//engine"],
)

rust_static_library(
    name = "engine_static",
    srcs = ["ffi_wrapper.rs"],
    deps = ["//engine"],
)
